# ============================================
# library.properties
# ============================================
name=IoT-Framework
version=5.8.0
author=IoT Team
maintainer=IoT Team <email@example.com>
sentence=Universal IoT Framework for ESP32
paragraph=Provides core modules for smart home devices: ConfigManager, LogManager, RTCManager, WiegandManager, AudioManager, MqttManager, WebServerManager, OTAManager, UserManager, PazManager
category=Communication
url=https://github.com/your-repo/IoT-Framework
architectures=esp32,esp8266
license=MIT
includes=IoTFramework.h
dot_a_linkage=true
# W3 (weather_gate): драйверы src/drivers/ безусловно включают <SPI.h>
# (Cc1101Driver) и <Wire.h> (Bme280/Ds3231 через BusManager). Без depends
# arduino-cli не добавляет include-пути SPI/Wire в профилях, где их не
# подтягивает ни один другой заголовок (поймано на test_rig, W3).
depends=SPI,Wire
